{
    "name": "JQSelect",
    "version": "1.0.0",
    "description": "A simple dropdown select by jQuery.",
    "license": "MIT",
    "scripts": {
        "dev": "gulp dev",
        "build": "rimraf dist && gulp compile:es && gulp compile:sass && gulp build",
        "example": "gulp example",
        "watch": "gulp compile:es && gulp compile:sass && gulp watch"
    },
    "devDependencies": {
        "babel-plugin-transform-runtime": "^6.23.0",
        "babel-preset-es2015": "^6.24.1",
        "browser-sync": "^2.18.2",
        "gulp": "^3.9.1",
        "gulp-autoprefixer": "^3.1.1",
        "gulp-babel": "^6.1.2",
        "gulp-cssmin": "^0.1.7",
        "gulp-plumber": "^1.1.0",
        "gulp-rename": "^1.2.2",
        "gulp-sass": "^3.1.0",
        "gulp-sequence": "^0.4.6",
        "gulp-uglify": "^1.2.0",
        "rimraf": "^2.6.1"
    }
}
